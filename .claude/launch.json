{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "nirvana",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; port=int(os.environ.get('PORT',3000)); s=socketserver.TCPServer(('',port),http.server.SimpleHTTPRequestHandler); s.serve_forever()"],
      "port": 3000,
      "autoPort": true
    }
  ]
}
